import { makeStyles } from "@mui/styles";

export const globalStyles = makeStyles(() => ({
  header: {
    backgroundColor: "#1976d2",
    height: 60,
    display: "flex",
    paddingLeft: 45,
    alignItems: "center",
  },
  rightIcon: {
    marginLeft: "auto",
  },
}));
